name: Deploy Camp

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    env:
      SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
      VPS_HOST: ${{ secrets.VPS_HOST }}
      VPS_USER: ${{ secrets.VPS_USER }}
      CAMP_PATH: /home/deploy/diabot_camp
      IMAGE: ghcr.io/DIABOT-dev/git:${{ github.sha }}
      DIABOT_URL: https://diabot.top

    steps:
      - uses: actions/checkout@v4

      # 1) Copy image name & trigger deploy qua SSH
      - name: Deploy to Camp Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ env.VPS_HOST }}
          username: ${{ env.VPS_USER }}
          key: ${{ env.VPS_SSH_KEY }}
          script: |
             set -e
             echo "🚀 Starting Deploy Camp"
             cd /home/ubuntu/diabot_camp || mkdir -p /home/ubuntu/diabot_camp && cd /home/ubuntu/diabot_camp


             echo "📦 Pulling latest image..."
             docker compose pull || true
             docker compose up -d || true

             echo "⏳ Waiting for services to start..."
             sleep 10

             echo "🩺 Checking service health..."
             curl -fsSL https://camp.diabot.top/api/qa/selftest || (echo "❌ Health check failed" && exit 1)

             echo "✅ Deploy Camp completed successfully!"

      # 2) Smoke test & log kết quả
      - name: Smoke test APIs
        run: |
          echo "Testing endpoints..."
          curl -s -o /dev/null -w "%{http_code}" ${DIABOT_URL}/api/qa/selftest
          curl -s -o /dev/null -w "%{http_code}" ${DIABOT_URL}/api/chart/7d
          curl -s -o /dev/null -w "%{http_code}" ${DIABOT_URL}/api/profile
        continue-on-error: true
